package nl.knokko.customitems.recipe;

/**
 * Used to denote an ingredient, which is either a vanilla item or custom item.
 * Because the editor and plug-in code don't have any methods in common for this,
 * this interface doesn't define any methods.
 */
public interface SCIngredient {

}
