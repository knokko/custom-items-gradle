package nl.knokko.customitems.texture;

import nl.knokko.customitems.model.CollectionView;

import java.util.Collection;

public class CustomTexturesView extends CollectionView<CustomTexture, BaseTextureValues> {

    public CustomTexturesView(Collection<CustomTexture> liveCollection) {
        super(liveCollection);
    }
}
