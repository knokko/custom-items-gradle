package nl.knokko.customitems.projectile.cover;

import nl.knokko.customitems.model.CollectionView;

import java.util.Collection;

public class ProjectileCoversView extends CollectionView<SProjectileCover, ProjectileCoverValues> {
    public ProjectileCoversView(Collection<SProjectileCover> liveCollection) {
        super(liveCollection);
    }
}
