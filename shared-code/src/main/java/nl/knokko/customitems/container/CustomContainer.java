package nl.knokko.customitems.container;

import nl.knokko.customitems.model.Model;

public class CustomContainer extends Model<CustomContainerValues> {
    public CustomContainer(CustomContainerValues values) {
        super(values);
    }
}
