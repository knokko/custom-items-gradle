package nl.knokko.customitems.encoding;

public class ContainerEncoding {

    // Even though ENCODING_1 exists, it is never written down, so there is no reason to add it here

    public static final byte ENCODING_2 = 2;
}
